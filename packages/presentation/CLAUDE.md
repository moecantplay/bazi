# presentation package conventions

Pure TS view-models extracted from apps/web. Zero UI imports, framework-agnostic (no React dependency) — matches bazi-engine/content's convention. Never does chart math or content templating, only shapes/derives data already computed by those two packages for display.
