# Daymaster — project conventions

Co-Star-style daily-reading app on a BaZi (Four Pillars) engine. Local-only, no backend, installable PWA.

## Architecture
Package-specific constraints now live in each package's own CLAUDE.md (packages/bazi-engine/CLAUDE.md, packages/content/CLAUDE.md, apps/web/CLAUDE.md) so they load only when that directory is in play. `pnpm verify` (root) runs typecheck + lint + unit tests + build across all packages — commit only on green; see each package.json's `scripts` for the rest.

## Non-negotiables
- Never invent calendrical/astronomical constants: every table is embedded in `packages/bazi-engine/data/` with a source comment, computed via astronomy-engine, or copied from the brief §11. Unverifiable → flag in PROGRESS.md.
- Golden fixtures (brief §5) are authoritative; write tests first for engine work.
- Voice rules in VOICE.md bind all user-facing copy: no fatalism, no medical/financial/legal directives, agency line ends every daily reading.
- Deterministic reading selection: hash(birth data + ISO date). No render-time randomness.

## Decisions Log
- 2026-07-07 Tailwind 3.4 (not v4) — stable PostCSS pipeline with Next 15 static export, no relitigation of stack.
- 2026-07-07 Packages ship raw TS (`main: src/index.ts`) transpiled by Next via `transpilePackages` — no build step per package; `build` = `tsc --noEmit`.
- 2026-07-07 Day-pillar anchor: 1949-10-01 = 甲子 per brief §4.4.
- 2026-07-07 apps/web has no unit tests by design; logic lives in packages, web is covered by Playwright E2E.
- 2026-07-07 City dataset: GeoNames cities15000 (CC BY 4.0), top 2000 by population, regenerate via `node apps/web/scripts/generate-cities.mjs <cities15000.txt>`.
- 2026-07-07 VOICE.md written by orchestrator (register + hard rules); content-writer must comply, not rewrite it.
- 2026-07-07 Fire element accent (#D0662A, orange) deliberately distinct from cinnabar (#BF3A2B) so the seal stays the only cinnabar mass.
- 2026-07-07 Dark mode out of scope for v1 (one light theme done properly). SUPERSEDED same day: product owner promoted dark mode to M9 — bright-only background is off-putting; same design bar as light (AA contrast, seal-only cinnabar).
- 2026-07-07 Voice direction (M10): system jargon never stands alone — every mechanic term in user-facing copy is followed in-line by a relatable gloss (metaphor or everyday situation). Product-owner feedback: current copy reads too mysterious.
- 2026-07-07 Engine input conventions: instant = JS Date (absolute UTC); IANA zone string for local reading; dailyPillar takes "YYYY-MM-DD"; longitude east-positive; out-of-table years (pre-1900/post-2100) throw RangeError.
- 2026-07-07 EoT mean longitude from Meeus polynomial (source-commented) — accepted exception to "astronomy-engine only", see PROGRESS.md flags.
- 2026-07-07 astronomy-engine loaded via createRequire to dodge ESM/CJS dual-build inconsistency between vite and tsx.
- 2026-07-07 apps/web webpack config: extensionAlias .js→.ts (engine uses NodeNext specifiers) + NormalModuleReplacementPlugin shims node:module→shims/node-module.mjs so the engine's createRequire bundles for the browser.
- 2026-07-07 Post-review design decisions: cinnabar strictly seal-only (buttons = ink fill/paper text; day-column marker = ink underline); element hues never normal-size text (fills/swatches or ≥19px bold); input borders = --ink-soft (hairline is dividers only).
- 2026-07-08 "Show Chinese characters" settings toggle (product-owner choice over an English-first redesign): default on; off swaps glyphs for their English glosses and strips Han from reading text via content's stripHanCharacters (branch runs become animal names). The seal keeps its characters — it's the signature artwork, not copy. Stored at daymaster.han.v1 ("hide" = off, absence = on). SUPERSEDED 2026-07-17: default flipped to English-first (see below); the swap/strip machinery is unchanged.
- 2026-07-08 Service worker is build-finalized: scripts/generate-sw.mjs (postbuild, wired into `pnpm build`) injects the precache list (every file in out/) and a content-hash cache version into out/sw.js. Never hand-bump a version. Updates install-and-wait; only a user-accepted "Refresh" may reload (clients.claim on first install fires controllerchange too — guard stays).
- 2026-07-08 Push notifications are structurally out (web push requires an app server on both platforms; no backend). Retention = local streak (daymaster.streak.v1) + come-back-tomorrow chrome line. Chrome lines around readings are UI copy, not reading copy — the agency line still ends every reading.
- 2026-07-08 Share links encode the birth details in the URL (base64url JSON, /onboarding/?share=); a shared chart is always "someone to compare with", never a replacement profile. Share card is canvas-drawn on device in the current theme; cinnabar appears only inside the redrawn seal.
- 2026-07-08 Compare people: daymaster.people.v1 (named list) + daymaster.people-active.v1; legacy daymaster.compare.v1 auto-migrates to a person named "Them" on first Compare load. Backup JSON (versioned envelope, v1) is the local-only account substitute and carries profile + people + preferences; streak is deliberately excluded.
- 2026-07-08 iOS splash images (apple-touch-startup-image) deliberately skipped: dozens of viewport-pinned PNGs for one launch frame. Revisit only if installed-app polish becomes a priority.
- 2026-07-08 Icon pipeline: public/icon.svg is the single artwork source; scripts/generate-icons.mjs rasterizes any (192/512), maskable (content scaled to 0.78 safe zone), and opaque 180px apple-touch. scripts/generate-manifest-screenshots.mjs regenerates the install-sheet screenshots from the built export.
- 2026-07-08 M13 almanac direction (product owner, from a shared master-reading + Sinarmas almanac reference): all four features — per-activity day leanings, year/month/week outlook, date finder (1–2 charts, combined = min), generic 12-Day-Officers layer. Voice = layered guidance (VOICE.md rule 12): 宜/忌-style "Favors/Watch" chips on the surface, weather prose underneath, soft directives for small unregulated acts only. Activity set = hybrid (modern labels glossed with the classical category, M10 rule reversed). Almanac categories outside daily-app scope (burial, worship, stove set-up, fishing, hunting) deliberately unmodelled.
- 2026-07-08 Day-officer 宜/忌 table is a conservative common core across cited sources (see data/day-officer-tables.ts); per-publisher variance flagged in PROGRESS.md. Officer keyed at local noon like the other transit reads; a "week" is a 7-day aggregation view, not a divination unit.
- 2026-07-13 Glossary layer (product owner: readings need more context): every fact-tag caption is a link to a plain-language explainer — GLOSSARY in packages/content/src/glossary.ts keyed by ReadingLine.topic (set by every line builder), assembled from the same canonical glosses as the lines. UI = GlossarySheet bottom sheet + FactTag caption component; Today gets a "How this reading works →" read-more (READING_TOPIC entry). Sheets render through the Han toggle; entries are voice- and strip-checked in glossary.test.ts.
- 2026-07-13 Read-more deep dives (product owner, three rounds; Co-Star's read-more is the explicit model): two distinct layers behind a reading card. The caption link opens the glossary — category description only, never advice. A "Read more →" on the card opens a READ_MORE deep dive (packages/content/src/read-more.ts, keyed by ReadingLine.topic like the glossary) — an essay about the *reading* as lived (how the pattern shows up, what it makes cheap/costly), closing with a "Working with it" advice section; it never re-teaches the category (tested). All five interaction types have dives; advice obeys rule 12 (soft directives for small unregulated acts, no should/must); everything runs through the same voice/strip checks (read-more.test.ts). GlossarySheet renders both shapes; advice appears whenever the entry carries it, and only dives do.
- 2026-07-13 Voice refinement (product owner, three rounds): ten-god lines open on the classical name framed *as* a name ("The old calendars name this year Indirect Resource (偏印)…"), then translate it in full — a lived situation, not a four-word fragment. A term as bare subject ("The year's theme is the Indirect Resource note") is wrong; so is meaning-first with the term trailing (tried, owner reversed it); so is "the classics" as the source word (owner rejected — use "the old calendars"/"the old books"). Rule 11 also covers plumbing: the "each day/year carries one of the twelve animal signs" context lives in the glossary entry behind the caption (moved out of the lines once the glossary shipped, per owner — cards spend their words on the day), and inline branches always ride as branchToken "子 (rat)" so the Han strip keeps the sentence whole. TEN_GOD_PERIOD_THEMES in banks/horizons.ts carries the richer per-god themes (short glosses in vocab.ts stay for chips/captions).
- 2026-07-16 M17 Co-Star content restructure (owner: M15/M16 restyled but didn't rearrange; "original inspiration was Co Star"; loop research→execution→testing, see docs/research-2026-07-16-costar-layout.md): Today = hero (headline over day pillar) → At a glance (activities as dot-on-axis rows, watch←·→favors, strength = offset; neutral behind disclosure) → reading grouped by life area via ReadingLine.area set in the content assembler (transit → first natal palace; element/ten-god/star/stage → "overall"/"The day itself") with fact citations BELOW the prose Co-Star-style → Favors/Watch as stark word lists (pill chips retired) → week strip → agency. Palace-touch sentence retired — section headers carry it. Do/Don't naming still rejected (rule 12); tone and M16 Material skin unchanged.
- 2026-07-17 Glyph icon system (owner: English-only mode "looks like something is missing", proposed icons/images; three treatments mocked, owner chose element-hue icons over bare-ink and tinted-chips, plus fill-encoded polarity with wording kept): bespoke 24×24 fine-line set — 5 elements (leaf/flame/mountain/square-holed coin/droplet) + 12 zodiac animals — in lib/glyph-icon-paths.ts, rendered by components/glyph-icon.tsx (ElementIcon/AnimalIcon + bare GlyphMark variants for composing inside the orbit SVG). Element icons solid = yang / outlined = yin, colored var(--element-X) (element-fill rule, not text); ink tone inside the hero wash. Placed wherever a stem/branch shows in English mode (PillarColumns, PillarGlyph → horizon headers + finder, luck timeline, annual row, DayOrbit), always beside the wording, with aria-label + title; chart grid carries the "solid symbol = yang · outlined = yin" legend line. Han mode unchanged — characters stay the anchor. Stock icon packs rejected (license + style mismatch).
- 2026-07-17 English-first wording (owner, from on-device screenshots: "make the whole app wording as english first instead of mandarin first"): the Han toggle's default flips to OFF — glosses are the primary wording everywhere, characters are the opt-in. daymaster.han.v1 semantics inverted: "show" = on, absence = off (new default), legacy "hide" reads as absence so old devices land on the new default. Settings copy updated; E2E specs now assert glosses by default and opt in where they diff glyphs. Same round: pillar grid rebuilt as a 4-col × 6-row grid with per-column subgrid (grid-rows-subgrid) so stems/ten-god captions/branches/details share rows — fixes the day column's branch floating above its neighbours (it has no ten-god caption; flex columns drifted).
- 2026-07-17 Bold hero (owner: app reads flat/mono-toned; two rounds — flat 40% element tint shipped first, owner wanted more wow; researched Co-Star/M3-Expressive/2026 texture trends, owner picked a blend of three mockups, C-led): the Today hero is the screen's single color mass — an ink-wash `.hero-card` (stem element pools top-left 60→14%, branch element warms bottom-right 34%, via inline `--hero-stem`/`--hero-branch`; film-grain ::after overlay, inline SVG turbulence) under a 38px mixed-weight editorial headline (400 + extrabold middle run, `headlineRuns` in today-view — deterministic, presentational) over `DayOrbit` line art (64px stem centered in 1px ink rings, branch in a scalloped paper badge — the M3-shape nod — element node dots; glyphs obey the Han toggle, only the seal is exempt). All hero text full `--ink`; only large text in the stem's strongest zone (4.06:1 worst — large-text AA only); swatch dots dropped inside (same-hue dot can't hit 3:1 on its own wash). The 56px bare-pillar hero and at-a-glance element dots there are retired. Seal stays the only cinnabar; a Fire day's hero uses `--element-fire`.
- 2026-07-17 Today tweaks (owner, from screenshots): week strip promoted back under the date nav (M17's demotion reversed); at-a-glance dot-on-axis replaced by a bar growing from the center tick (the round dot read as a draggable slider thumb — no round marker on the axis, bare axis when neutral); streak line cycles fun wordings deterministically per calendar day (streakLine in apps/web/src/lib/streak.ts, FNV-hash of the date — never render-random).
- 2026-07-16 M16 Material look-and-feel (owner named Google's recent UI as the missing "something else"; chose "full Material feel" + "everything in tonal cards" + tinted surface): surfaces are --surface tonal fills, borderless in BOTH themes, separated by gaps — hairline survives only as a rare in-container divider; radius scale 24 (cards) / 28 (sheets) / 16 (inputs) / pill (buttons, chips, segmented); lists are segment stacks (2px gaps, first/last corners 24px); chips are a 10% ink tint via color-mix (.bg-ink-tint — tokens are raw hex, Tailwind /alpha doesn't apply); Figtree replaces Fraunces AND Inter — display is the same family at 700/-0.01em, agency/archetype at 600 (emphasis), section headers sentence-case 13px/600 with the M15 ink column-rule retired; springy 240ms overshoot on selections/sheets, reduced-motion gated. SUPERSEDES the M15 ruled-prose/hairline surface language; keeps M15's a11y decisions (ink-soft control borders both themes, ≥44px tap targets, pressed states), the guidance grouping, the gauge disclosure, and the untouched identity: ink/cinnabar palette, seal-only cinnabar, element hues, voice.
- 2026-07-16 M15 design refinement (owner unhappy with design/UI/layout; direction = calm minimal refinement, not a new look): four-register type ladder plus the Han register (display / body 15px ink / kicker under a 24×2px ink column-rule — the app's recurring quiet signature / 12px caption; 16px input text is the named exception, iOS zoom); links and captions never underlined (the day-column "it's you" marker is the sole underline and isn't a link) — captions are chevron-metadata tap targets, nav links use the arrow as affordance, both ≥44px targets with an :active pressed shift; surfaces split into ruled prose (reading content: no box, hairline rules between blocks) vs boxes (non-interactive functional objects only — board, agency line, decade card, sheets; inputs are NOT boxes); guidance prose groups by fact tag so an officer day reads as one block, not three sibling cards; forms spec (.field-input 48px with --ink-soft border in BOTH themes, SegmentedControl for 2–3-way choices, picker rows = raised list with hairline dividers + 2px inset ink ring when selected, disabled primary = raised paper, "unpressable not broken"); dark theme separates boxes by fill (.dark-borderless), hairlines dividers-only; interactive controls keep --ink-soft borders in both themes and the agency line's ink top-border is the sole decorated box (reviewer carve-outs). SUPERSEDES one 2026-07-14 detail: the areas section now shows leaning rows with neutral rows behind a "Show all N areas" disclosure, not all 10 gauges unconditionally.
- 2026-07-14 Co-Star arrangement for Today (product owner, full shape chosen from three mockups): headline hook opens the screen (banks/headlines.ts — keyed off a shown daily transit first, then annual/monthly, then element day, then generic; pure voice, cites nothing, no Han); Favors/Watch merged into one two-column board (chips + the fact-cited suggestions per column, guidance prose beneath — rule-12 labels kept over Co-Star's literal Do/Don't so chips never read as prohibition); "Your day, by area" gauges all 10 activities (meter = score magnitude in the finder's wood-green/fire-orange tints, tap unfolds activityAreaLine, which reuses the chip-explanation frames so a chip and its row never disagree; neutral rows get NEUTRAL_AREA_FRAMES); week strip demoted below the areas; agency line still ends the reading.
- 2026-07-17 Chinese characters removed entirely (owner, from on-device screenshots; SUPERSEDES the Han toggle decisions of 2026-07-08/07-17): no Settings toggle, no HanCharactersProvider, no daymaster.han.v1; English glosses are the only register and reading text always runs through stripHanCharacters. Romanized names stay only where no direct translation exists and always with the meaning beside them ("rén water · chén earth"). Old backups carrying the retired preference still import; the field is ignored.
- 2026-07-17 Seal → personal logo (owner picked L3 from four mocked directions, after rejecting all character-seal variants): day-master element icon inside an orbit ring on the cinnabar square, yang solid / yin outlined, ring weight + node position + rotation + corner notches still FNV-seeded per chart — a logo that stays a signature. App icon (public/icon.svg) swaps 日 for the generic orbit mark (ring + day disc + node); share card unaffected structurally (clones the live SVG). Cinnabar remains logo-only.
- 2026-07-17 Zodiac animal icons v2 (owner rejected the abstract line faces AND three rounds of hand-authored redraws; source = owner-run Gemini Nano Banana Pro silhouettes): filled single-path silhouettes in lib/animal-icon-paths.ts, produced by a trace-and-polish pipeline (potrace vectorize → paper.js curve simplification → mirror to uniform left-facing → refit 24-box) — recognizability comes from the generated source, design cohesion from the geometric polish, never from LLM-invented anatomy. Dragon ships two paths: compact default, detailed ≥40px (renderSize prop). Pillar grid seats icons in 48px bg-ink-tint circles with 13px wording beneath (icon is the anchor, word the caption); legend now reads "element icon solid = yang · outlined = yin" since animals are always solid.
- 2026-07-29 v2 arc approved (owner; full discussion in docs/discussion-2026-07-23-rewrite-and-roadmap.md): M18 design reset → M19 apps/web rebuilt from empty (engine/content/fixtures KEPT — the "rewrite" is the shell only) → M20 backend+accounts → M21 subscriptions → M22 Expo mobile (App Store + Play Store). Governing principles: readings computed on-device forever (backend = identity/sync/entitlements, never reading generation); "local-only" becomes "local-first" (signed-out app stays fully functional — don't design new features that assume no server, and don't gate the core on an account); ONE visual identity chosen from three on-device mockups (editorial-stark / full Material / modern almanac) before any app code — current look diagnosed as an unsatisfying blend of the almanac identity and M16 Material. M19 folds in structural debt: packages/presentation view-models, content as structured token runs (stripHanCharacters retired), single versioned store shaped like the sync payload. Payments: Stripe web, RevenueCat for store IAP, entitlements server-side. Supersedes "push notifications structurally out" (2026-07-08) once M20 lands. M18 entry gate: owner supplies 3–5 reference-app screenshots + designer-budget decision.
- 2026-07-30 Both themes are one design, not a light design plus a dark port (owner reminder mid-M18: "we have a light and dark mode"). Rules for the mockups and for DESIGN.md v4: (a) every direction is reviewed in BOTH themes on device — mockups carry a Light/Dark/System control, and a forced theme must resolve identically to the matching OS scheme (scripted computed-style diff, not eyeballing); (b) contrast is computed for both themes over rendered text, not token pairs — the light side has been the weak one every time (all 34 M18 failures were light-mode: muted captions and element hues at 8.5–13px); (c) a high-contrast "anchor" mass is defined by distance from the ground, not by the colour black — in dark the anchor inverts to paper-with-ink text rather than staying near-black, so it remains the boldest object on the screen; (d) no theme rule may live only in `@media (prefers-color-scheme: dark)` — every one needs both `[data-theme]` twins or the user's own toggle can't reach it.
- 2026-08-05 M18 direction locked: **Trail** (the day as a route on a topographic map), chosen over Daybreak, Sky, Compass, Compass II, and Forecast. DESIGN.md rewritten to v4 from docs/design-system/ (the built-out foundations + Trail + Components card set, 17 cards / 3310 rendered text runs / 0 contrast failures / theme-parity clean — `node docs/design-system/check.mjs`). Rejected directions and why: Daybreak (editorial-air) and Sky (soft dashboard) read as generic wellness-app skins once built past one screen; Compass/Compass II (warm cream, diagram-led) lost to Trail's stronger single motif (the dashed route reads as wayfinding, the diagram didn't generalize past the hero); Forecast (weather-report framing) fought the existing voice's "not a verdict" stance harder than Trail's neutral map framing does. The effects-exploration pass (glass/neumorphic/gradient, docs/research-2026-07-30-*.md) was run against Daybreak's layout, not Trail's — none of its recommendations (gradient-extended, neumorphic accent) carry into this rollout; a similar minor-detail pass on Trail is a future decision if wanted, not assumed.
- 2026-08-05 Rollout scope decision (owner, when asked to choose between a fast reskin and folding the M19 structural rebuild into the same pass): **reskin apps/web in place**, same pattern as the M15/M16/M17 passes — apply Trail's tokens/type/components to the existing app. This *reorders* the approved v2-arc sequencing (CLAUDE.md 2026-07-29): M19's structural rebuild (packages/presentation, structured content tokens replacing stripHanCharacters/branchToken, single versioned store) is deferred, not folded into this milestone. M18's original "zero app code changes this milestone" line is superseded by this choice — the owner asked to roll Trail out immediately rather than sequence through a full rebuild first.
- 2026-08-05 Icon and asset continuity: Trail reuses the existing bespoke icon files as-is (`lib/glyph-icon-paths.ts` element set, `lib/animal-icon-paths.ts` zodiac silhouettes) — only their color binding moves from `--element-*` to the new theme-keyed hue tokens (`--wd`/`--fr`/`--er`/`--mt`/`--wt`). The simplified icon paths inlined in `docs/design-system/src/sprite.html` were prototype-only (that file has to be a self-contained HTML preview card with no sibling dependency) and are never a second source of truth. The seal is unchanged and stays the only cinnabar mass, on `--paper` only, in both themes and across all five terrains.
- 2026-08-05 Anchor-mass rule resolved for real app code (open "FINDING" in docs/design-system/src/cards/app.mjs's buttons card): Trail's "one black anchor per screen" rule and a screen's primary CTA button never actually compete, because the signpost board only exists on Today, and Today has no primary button (it's a read-only daily reading) — every other screen's primary button is simply that screen's sole anchor. No further resolution needed; documented in DESIGN.md §Surfaces so it isn't re-litigated as a bug.
- 2026-08-05 M18.5 Trail rollout shipped (all 4 waves; DESIGN.md v4 fully implemented in apps/web). Reviewer critique on DESIGN.md itself (before rollout) found one real gap folded back into the spec: "one black anchor per screen" was self-contradicted by the always-present anchor-filled bottom nav — resolved by splitting the anchor pair into a fixed chrome anchor (the nav, outside any per-screen budget) and a per-screen content anchor (signpost/primary button); no visual change, just closing the spec gap before Wave 2 built it. A live rendered-contrast pass against the built app (not just docs/design-system's own prototype cards) across all 5 terrains × both themes found 3 real AA failures the prototype hadn't caught, all fixed: elevation-profile's day labels fading below AA along with their distance-fade icon (only the icon fades now); segmented-control's unselected label at ~4.0:1 against its own ink-soft-tinted track (now full ink); legend-tags reusing the `.chip` class's ink-tint background, too dark for ink-soft text at 9.5px (now a plain `bg-surface` pill, matching the untinted mockup chip). Lesson consistent with every prior design round in this project: token-level verification (check.mjs on the prototype) does not substitute for measuring the actual shipped composition — opacity, shared-class reuse in a new context, and terrain-shifted tokens each independently reintroduced failures the prototype's own gate had already closed. A second code-review pass (correctness/a11y/dead-code, not the design doc) returned APPROVE with 2 trivial cleanup items (dead M17 `.hero-card` CSS, stale PWA `themeColor` hex pair), both fixed. Final state: `pnpm verify` green, 24/24 E2E green, 0 contrast failures across 6 screens × 5 terrains × 2 themes.
- 2026-08-05 M19 (apps/web rebuild) started, from-scratch as originally planned (owner confirmed, over an in-place-evolution alternative that was raised given M18.5 already left apps/web clean — the owner chose to keep PLAN.md's original shape: new app directory, old app stays deployed until parity, cutover last). Six implementation decisions locked (full plan: /Users/Tipitaka/.claude/plans/woolly-hugging-taco.md, phases mirror this): (A) content-type changes are additive — `ReadingLine`/`GlossaryEntry`/`ReadMoreEntry` gain new `runs`/`factTagRuns` fields alongside the untouched `text`/`factTag` strings, so the still-deployed old app's 7-8 render call sites never break and root `pnpm verify` stays green through the whole rebuild; cleanup (deleting the string fields + `strip-han.ts`) happens only at the very end, once the old app is about to be retired. (B) `packages/presentation` stays React-free (matches bazi-engine/content's "zero UI imports" convention) — `use-today-label.ts`'s browser-event logic splits, pure date-math in presentation, a thin hook in the new app. (C) The streak counter (`daymaster.streak.v1`) does NOT join the single store — stays a deliberate, documented carve-out (unifying it would make a future multi-device sync fork/clobber a device-local habit count); `delete-my-data` clears both keys as one action. (D) New app lives at `apps/web-next` during development; `apps/web` (and its Vercel project) stays untouched until cutover (delete old, `git mv` new into place, flip Vercel's root directory — the only manual step). New app's E2E reserves port 3211 (old app keeps 3210). (E) Token-run conversion (packages/content's structured `{text}`/`{term,gloss,han}` runs replacing Han-embedded prose + `stripHanCharacters`) is a hybrid: one centralized pass on `vocab.ts`'s `branchToken`/`interactionTag` (covers every bank's fact tag at once) then bank-by-bank for the actual prose offenders — `banks/transit-days.ts`, `banks/stars.ts`, `banks/stages.ts`, `glossary.ts`, and `vocab.ts`'s `STRENGTH_CHECK_GLOSSES` (a fourth Han-embedding shape found this session, mixed English+Han parenthetical). Grepping every bank for literal CJK found most of the original "13 banks" estimate was false positives (object keys, doc comments) — real surface is 5 units of work. (F) A term run's `han` field is typed (matches the brief's shape, forward-compatible) but no presenter renders it this milestone — Han display stays fully retired per 2026-07-17, this is not a regression to "fix."
- 2026-08-05 Live production bug found and fixed (M19 Phase 8, apps/web-next build): `--cinnabar` was referenced by `tailwind.config.ts`/`seal.tsx`/`glossary-sheet.tsx` in BOTH apps but never actually defined by the Trail token generator or `globals.css` in either one — the seal, DESIGN.md's one explicitly fixed identity element ("the seal is the only cinnabar mass, always"), had been silently rendering plain black instead of cinnabar red since the M18.5 Trail rollout shipped it. Fixed with a one-line addition to each app's `globals.css` `:root` (`--cinnabar: #bf3a2b`, the 2026-07-07 decision-log value) — apps/web-next first (where it was found), then apps/web immediately after since that's the still-*live* deployed app, not just an in-progress rebuild. Confirmed via the running dev server that the token resolves correctly, then a full clean rebuild + `pnpm verify` + 24/24 E2E on apps/web. Lesson: this slipped past every M18.5 review pass (design-spec review, live rendered-contrast check across 5 terrains × 2 themes, a full code-review pass) because none of those checks assert a referenced CSS custom property is actually *defined* — they all measure computed contrast on properties that *do* resolve to something. Worth a "every `var(--x)` reference has a defining rule" grep-style check in any future design-system audit.
- 2026-08-05 M19 Phase 11 reviewer pass: REQUEST_CHANGES → fixed. One real blocker: `vocab.ts`'s `interactionTagRuns` and `compare-reading.ts`'s `compareTagRuns` concatenated multiple branch term runs with no separator — under TokenText's gloss-only render mode (M19 decision F) this fused adjacent branch names into one word ("子午 clash" rendered as "rathorse clash" instead of "rat–horse clash"), a regression the additive-runs migration (decisions A/E) had silently carried since Phase 4/5 and no test had caught because `fact-tag.test.ts` only asserted the untouched plain-string `.factTag`, never rendered `.factTagRuns` through gloss-only concatenation. Fixed with a shared `joinBranchRuns()` helper (en-dash separator, matching `stripHanCharacters`'s BRANCH_RUN join exactly) both functions now delegate to; added `packages/content/test/vocab.test.ts` plus a `plainGloss()` test helper that renders a TokenLine the way the UI's TokenText presenter actually does, as the permanent regression guard. Two minor fixes alongside it: `pillar-columns.tsx` was reimplementing `packages/presentation`'s already-exported `starsForPalace` inline instead of importing it (dead duplication from before the extraction was wired back to its caller); `settings-content.tsx`'s delete flow called both `deleteAllData()` and a separate `clearStreak()`, redundant since `deleteAllData()` already clears the streak key internally per decision C. Root `pnpm verify` green and both E2E suites (24/24 old app, 28/28 new app) re-confirmed green after all three fixes.
- 2026-08-06 M19 Phase 11 content cleanup executed (owner chose "fold into Phase 12" sequencing over a standalone step): `ReadingLine.text`/`.factTag` string fields, `strip-han.ts`/`strip-han.test.ts`, and the string-returning `branchToken`/`interactionTag` deleted outright — `runs`/`factTagRuns` are now the package's only public shape. Introduced a `DraftLine`/`finalizeLine()` boundary in `packages/content/src/types.ts`: every bank/assembler builds an internal `DraftLine` (optional `text?`/`factTag?`/`runs?`/`factTagRuns?`), and `finalizeLine()` is the single place that becomes the public, runs-only `ReadingLine` — this is what let every source file across content/presentation/apps-web-next convert mechanically without re-deriving the fallback logic per call site. Taking the `stripHanCharacters` safety net away surfaced two more real, previously-invisible Han leaks: `compare-reading.ts`'s `dayMasterLines()` had two fact tags with zero `runs`/`factTagRuns` authored at all (new `stemTokenRuns()` helper fixes it); `natal-interactions.ts`'s body text never authored `runs` for its `{branches}` placeholder, filling it with raw joined Han (fixed via `fillRuns()`+`joinBranchRuns()`). A third bug surfaced by the test-migration pass itself, not by any prior review: `horizon-reading.ts`'s `themeLine()` assumes every `THEME_FRAMES` template keeps `"{tgEn} ({tgCn})"` contiguous so it can collapse the pair into one term run, but `THEME_FRAMES[1]` had `" note "` splitting them — the collapse silently no-op'd and `{tgEn}`/`{tgCn}` leaked as literal unrendered braces into real annual/monthly readings (verified live, english "Rob Wealth"/seed "seed"); fixed by reordering the template's wording to restore the invariant rather than making the collapse logic itself more permissive, since the invariant is cheap to hold and documented in a comment right above the code that assumes it. Lesson consistent with the `--cinnabar` finding (2026-08-05): a safety net (`stripHanCharacters`, additive fallback wraps) makes a whole class of authoring gaps invisible to every review pass and to production, and removing it is often the only way those gaps surface — worth treating "does removing the safety net reveal anything" as its own explicit verification step whenever a defensive fallback is finally retired, not just trusting that prior green runs proved the underlying data was already correct. Full reverification after all fixes: `apps/web-next` + `packages/{bazi-engine,content,presentation}` green end-to-end (typecheck/lint/401 unit tests/build/28 E2E); `apps/web` now fails typecheck (27 errors, all the removed string API) exactly as expected, since it's deleted next in Phase 12 rather than fixed.
- 2026-08-06 M19 Phase 12 cutover executed: old `apps/web` deleted (`git rm -r`), `apps/web-next` moved into its place (`git mv`, then a manual fixup since the old app's gitignored build artifacts — `.next`/`node_modules`/`out`/`test-results`/`tsconfig.tsbuildinfo` — were still on disk and made `git mv` nest the new app a level too deep; corrected by moving the nested directory up and discarding those stale artifacts, all safely regenerable). Package identity updated for its new location: `@daymaster/web-next` → `@daymaster/web` in `package.json`, E2E port `3211` → `3210` (no more coexistence need — the old app and its 3210 reservation are gone), doc-comment references to `apps/web-next` swept to `apps/web` across `playwright.config.ts`/`static-server.mjs`/the three PWA generator scripts/`store-migration.spec.ts`/`render-run.ts`, `apps/web/CLAUDE.md` rewritten from "M19 in-progress, don't touch the old app" to describe itself as the current app. `pnpm-lock.yaml` refreshed via `pnpm install`. Full reverification at the new location: `pnpm -r typecheck/lint/test/build` green (401 unit tests), 28/28 E2E on port 3210, and a live `next dev` sanity check (`/`, `/today/`, `/chart/`, `/cycles/`, `/compare/`, `/dates/`, `/settings/` all 200, real page titles, not error pages) — the running-app check this project's conventions require beyond green tests. All work from M18 through this cutover was committed in four commits just before the cutover itself (design reset, Trail rollout, M19 rebuild, decision-log docs) once it became clear the entire session's work had been sitting uncommitted, which would have made the cutover's deletion of the old `apps/web` unrecoverable if anything had gone wrong — commit before any destructive step when uncommitted work has been accumulating, not just before the step itself. Correction to the M19 plan's decision D: there is no Vercel root-directory setting to flip — per the 2026-07-17 deploy setup, this project ships via a CLI static deploy of `apps/web/out` with no git integration, so a plain `pnpm --filter @daymaster/web build` + `vercel link`/`vercel deploy --prod` from `apps/web/out` already deploys the new app now that it lives at that path — deploy/production verification is the one remaining step, not a dashboard change.
- 2026-08-21 Per-activity terrain gauge revived on Today (owner request; the old M13/M14 "Your day, by area" gauges had been dropped, unremarked, by the M18.5 Trail reskin — DESIGN.md's retirement note only ever named the axis-dot rows and the Favors/Watch board, not a decision to drop per-activity detail entirely). Reviewed as an on-device mockup first (light/dark toggle, matching this project's own mockup convention) before any app code, per the 2026-07-30 both-themes rule. Shipped as `packages/presentation/src/activity-terrain.ts` (`activityTerrain()`, pure, reuses `elevationPath()` — widened to a minimal `{x,y}` structural param so both the 7-day and this 10-activity plot share the same path-join logic) feeding `apps/web/src/components/activity-terrain.tsx`: a "skyline" across all 10 activities in the shipped `ElevationProfile`'s exact dashed-line/tone-height grammar, collapsed by default with a "Show all 10 in detail" disclosure (chart-view.tsx's existing pattern, no new animation infra) into a full manifest list. No engine or data-fetching change was needed — `dayQuality()` already computed all 10 `ActivityAssessment`s every render; only `guidance.chips` (top-3, favors/friction only) was ever wired to a component. Real bug caught during mockup review and designed around from the start: an SVG with `preserveAspectRatio="none"` (needed so the dashed line stretches to fill a wide, short card, same as the shipped 7-day profile) scales x/y non-uniformly — putting circles or rotated text *inside* that space turns circles into ellipses and shears text. Fixed/avoided by following `ElevationProfile`'s own split exactly: the SVG holds only the dashed path; the 10 dot markers and angled key-word labels are ordinary HTML elements positioned by percentage on top. DESIGN.md's Today layout section updated to note this sits between the waypoint-rail reading and trail signs, and that trail signs remain the curated top-3 summary while this is the full 10-activity detail underneath it, in the same visual grammar rather than a revival of the retired axis-dot/Material board.
