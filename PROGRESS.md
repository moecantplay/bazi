# Daymaster — PROGRESS

Living checklist. Every checked task carries a one-line evidence note.

## M0 — Scaffold
- [x] git init (main branch) — repo initialized
- [x] pnpm workspace: apps/web, packages/bazi-engine, packages/content — pnpm-workspace.yaml
- [x] Root `pnpm verify` script (typecheck+lint+test+build) — package.json
- [x] pnpm install green — pnpm 9.15.9, Next 15.5.20 resolved
- [x] PLAN.md / PROGRESS.md / CLAUDE.md written
- [x] 5 subagents defined in .claude/agents/ — engine-dev, content-writer, ui-dev, reviewer, test-runner
- [x] Initial commit on green verify — full `pnpm verify` green (typecheck+lint+test+build, static export OK)

## M1 — Engine core
- [x] Types + reference tables in data/ — char-by-char reviewer-verified vs §11
- [x] Solar-term generator + data/solar-terms.json (1900–2100) + anchor tests — 2412 entries, strictly increasing, anchors within ±1d
- [x] sexagenary / yearPillar / monthPillar / dayPillar / hourPillar / annualPillar / dailyPillar — 50 tests green
- [x] EngineConfig (lateZiHour, trueSolarTime) — fixture D + EoT bounds tested
- [x] Golden fixtures A–D green — reviewer independently recomputed boundaries (立春 2000 to the ms)

## M2 — Engine derived
- [x] hiddenStems / tenGods — reviewer re-derived full 10×10 matrix, exact match
- [x] interactions (combines/clashes/trines/punishments/harms + palaces) — duplicate-branch pairs verified
- [x] luckPillars (direction + start age + 8 pillars) — fixture A: 114 months → age 9 / 2004, recomputed by reviewer
- [x] strength / favorableElements — labeled interpretive, one school; fixture A weak, [fire, earth]
- [x] ReadingFacts emitted — natalFacts + dailyFacts deterministic, annual+daily transits
- [x] ≥20 total fixtures, coverage ≥90% — 90 tests, 97.7% lines
- [x] Follow-ups: >2100 RangeError guard + backward-luck startAge assertion — 98 tests green, committed

## M3 — Content
- [x] VOICE.md — packages/content/VOICE.md (hard rules, calibration, palace vocab, disclaimer copy)
- [x] Natal bank (114) / daily bank (62 templates → ~139 rendered variants) / luck transitions (10) — reviewer APPROVE, committed
- [x] Deterministic seeded selection — FNV-1a, determinism tests
- [x] Disclaimer copy — byte-identical to VOICE.md, asserted in tests

## M4 — UI shell + onboarding
- [x] DESIGN.md + reviewer critique — reviewer APPROVE after 6 fixes (AA element-hue rule, seal-only cinnabar, ink-soft input borders)
- [x] Onboarding flow incl. unknown-time + city picker (cited dataset) — reviewer APPROVE; 14/14 headless flow checks, zero console errors
- [x] Seal component — pure FNV-1a/mulberry32-seeded SVG, cinnabar-only mass
- [x] localStorage persistence — daymaster.profile.v1 via single lib/profile.ts gateway
- [x] Settings screen (full) — toggles persist immediately, delete-my-data with confirm (built early, ahead of M6)
- [x] City dataset — 2000 cities from GeoNames cities15000 (CC BY 4.0, cited), 180KB, generator script apps/web/scripts/generate-cities.mjs
## M5 — Chart + Today
- [x] Chart screen (grid, day-master, balance, favorable, seal) — reviewer APPROVE; stable section keys
- [x] Today screen (±30d nav, palace touches) — UTC-safe date math, agency line last/distinct
- [x] Fixture A renders 甲戌 丙子 戊辰 庚申 + trine + Eating God — headless-verified on the built export

## M6 — Cycles + settings + PWA
- [x] Cycles screen (Fixture A start years correct) — headless-verified 丁丑@9/2004, 己卯 decade highlighted, 2026=丙午; reviewer APPROVE
- [x] Settings (toggles, disclaimer, delete data) — shipped in M4
- [x] PWA manifest + SW, no console errors — trailingSlash export, route precache verified, installability basics green

## M7 — E2E + README + DoD audit
- [x] 3 Playwright flows green — onboarding→chart, chart→today→date-nav, settings-toggle-changes-output; 3/3 against the built static export, re-run by orchestrator
- [x] README + clean-clone verify — fresh clone → pnpm install → pnpm verify GREEN (scratchpad clean-clone)
- [x] Full DoD re-audit green — see DoD audit below

## DoD audit (final, 2026-07-07)
1. [x] pnpm verify green from clean clone — fresh clone in scratchpad, README steps, GREEN
2. [x] §5 golden tests pass; fixtures ≥20; engine coverage ≥90% — 98 engine tests green; 97.77% lines / 96.44% branch (threshold enforced in vitest config)
3. [x] Fixture A renders 甲戌 丙子 戊辰 庚申 + Water trine + Eating God — E2E onboarding-to-chart.spec.ts
4. [x] 3 consecutive daily readings differ, each cites ≥1 computed fact — E2E chart-today-datenav.spec.ts (data-fact-tag assertions)
5. [x] Unknown-time → 3-pillar chart, no hour-dependent copy — engine unknown-time fixtures (no hour palace/facts), content tests (no hour phrasing), UI "hour unknown" frame verified headlessly
6. [x] Cycles shows Fixture A luck pillars with correct start years — 丁丑@2004 … verified headlessly against engine goldens
7. [x] 3 Playwright smoke flows green — 3/3, single worker, pinned clock, real static export
8. [x] PWA installable + no console errors — manifest + SW verified on the served export (fetchable manifest, SW controls page, icons resolve, offline fallback); zero console/page errors across every headless drive (14/14, 10/10, 8/8 checks)
9. [x] Disclaimer in onboarding + settings; VOICE holds on 20-line random sample — disclaimer byte-identical (tested); reviewer sampled 20 lines across banks, all compliant
10. [x] README: what/quickstart/architecture/doctrine/screenshot — docs/screenshot-chart.png captured from the real /chart

## M9 — Dark mode
- [x] DESIGN.md dark section — token table with computed WCAG ratios (ink 14.40:1, ink-soft 7.87:1, element hues 5.87–6.90:1), seal-only cinnabar preserved via new `--seal-paper` token (interior stays stamped-paper white; corner notches follow the page)
- [x] CSS-variable dark theme: `prefers-color-scheme` default, `data-theme` pin from Settings (System/Light/Dark radiogroup), `daymaster.theme.v1` in localStorage, pre-paint inline script so pinned themes never flash, PWA theme-color per scheme, delete-my-data clears it
- [x] Playwright theme spec — pin dark, survive reload, Light beats dark OS, System follows OS; 4/4 E2E green, verify green

## M10 — Plain-meaning voice pass
- [x] VOICE.md rule 11: every system term followed by a plain-life gloss — calibration examples added, "naked jargon" listed as a wrong
- [x] Central gloss maps in vocab.ts (INTERACTION_GLOSSES, TEN_GOD_GLOSSES, DAY_MASTER_GLOSS, LUCK_PILLAR_GLOSS), exported from the package
- [x] Banks rewritten against the rule: transit/natal interactions, ten-god day lines, strong/weak day-master, luck templates — day-master archetypes already compliant
- [x] Chart jargon translated in UI: ten-god captions under pillar stems get short glosses, day-master heading gets its gloss, Cycles timeline opens with the luck-pillar gloss
- [x] Voice test asserts every gloss exists and is voice-clean; 45 content tests green, verify green, 3/3 E2E

## M8 — Compare (stretch, done)
- [x] Engine: compareInteractions + compareFacts — cross-chart PAIR relations only (combines/clashes/harms/子卯 punishment/mirror self-punishments/trine pairs), day-master ten gods both directions, element support; 12 tests written first, hand-derived vs the 1949-10-01 甲子 anchor chart; 110 engine tests green
- [x] Content: compare bank + compareReading — relation lines per element cycle, seen-as lines reusing TEN_GOD_GLOSSES, cross-interaction templates with M10 glosses, mirror-punishment phrasing, ≤3 seeded interaction lines; all templates in the voice sweep; 51 content tests green
- [x] Web: /compare screen (5th tab) — compact second-birth form (unknown-time path, city picker, sex), companion persisted at daymaster.compare.v1, same engine config as primary, both pillar grids + cited reading cards, change-person clears; delete-my-data clears companion too; SW precaches /compare/ (cache bumped v2)
- [x] E2E compare flow (fill → read pair → reload persists → change person clears); 5/5 flows green, verify green

## M11 — Master-reading parity (life stages, stars, na yin, strength detail, do's/don'ts)
- [x] Golden fixtures transcribed from a professional charting app's reading of Fixture A (test/master-reading.test.ts, 18 tests): life stages incl. 自坐 and transit stages, per-pillar 神煞, 空亡=戌亥, strength 失令/得地/失勢, luck start 9y5m25d, na yin, 胎元 丁卯
- [x] Engine: twelve life stages (day-master + self-sitting per pillar), 納音 30-name table, 神煞 module (19 stars incl. 空亡; dual year/day keying for trine-group stars per classical practice), 胎元, strength breakdown (seasonalSupport/rooted/backed = 令/地/勢), luck start to day precision (3d=1y → 6h=1m → 1h=5d), transitBranch on transit facts; 128 engine tests green
- [x] Content: STAR_GLOSSES/LIFE_STAGE_GLOSSES/STRENGTH_CHECK_GLOSSES in vocab, stars + stages banks, strength why-line (three checks in plain words), transit templates rewritten to name which branch is today's vs yours (how/why/what), do's & don'ts (1–2 each, fact-cited, generic fallback, postponement-not-prohibition); 56 content tests green
- [x] Web: per-pillar stage/sound/star captions with glosses, Your stars section, 胎元 line, precise 起運 line on Cycles, Worth doing / Worth postponing cards on Today; verify green, 5/5 E2E

## M12 — Hardening, retention & ownership sweep (2026-07-08)
- [x] Show Chinese characters toggle finished and shipped — provider reads localStorage before first paint (no flash; all consumers gated), stem+branch pillar pairs strip to plain names, E2E covers chart/today/cycles/compare + persistence
- [x] Correctness: Today re-anchors past midnight on focus/visibility (E2E simulates rollover); storage writes guarded (private browsing shows a plain-words note instead of crashing onboarding); dates format in the device locale (E2E pins en-GB)
- [x] Service worker rebuilt: precache + cache version generated from the build output (all 64 files incl. chunks and RSC payloads — never-visited routes render offline, E2E-proven), no manual version bump, updates wait for a user-accepted "Refresh", offline navigations fall back to /today/
- [x] Data ownership: Edit birth details from Settings (reuses onboarding steps, confirm shows recomputed pillars, preferences untouched); Download my data JSON backup + Restore from file on onboarding; delete confirm names everything it erases — E2E round-trips all three
- [x] Onboarding: draft + step persist to sessionStorage (refresh resumes); step headings take focus for screen readers (no visible ring — globals exempt tabindex="-1")
- [x] Today: tap the date → native picker clamped to ±30d, boundary explains itself, aria-live on day changes; streak line ("N days running", localStorage, cleared by delete); come-back-tomorrow note as chrome after the agency card. Push notifications ruled out structurally (no backend); badge API skipped (nothing sets one)
- [x] Compare: named saved-people list (daymaster.people.v1, auto-migrates the legacy single companion), switch without re-entry, per-person remove; companion date formatted; E2E add/switch/remove + migration
- [x] Chart progressive disclosure: day-master anchor first, stage/sound/star captions behind "More pillar detail", structure/stars collapsed with counts; DoD trine assertion now opens the section first
- [x] Share: card image (seal + pillars + archetype on canvas, Web Share with download fallback, type steps down to fit) and chart link (birth encoded in URL, recipient recomputes; with a profile → Compare prefilled, fresh device → waits for after onboarding); E2E download + link round-trip
- [x] PWA polish: icon artwork fixed (glyph inside frame), any/maskable split + opaque 180px apple-touch icon (generator script), manifest id/shortcuts/screenshots (generator script), Settings install hint (beforeinstallprompt button when available). iOS splash images deliberately skipped
- [x] Perf: cities.json (180KB) is its own post-mount chunk — onboarding first-load JS 266→228kB, compare 269→229kB
- [x] A11y/audit: main landmark on all shell screens; every screen screenshotted light+dark at 390px and reviewed; full verify green; 15/15 E2E (10 consecutive clean full-suite runs)

## M13 — Almanac & horizons (date selection, day leanings, year/month/week outlook) (2026-07-08)
- [x] Engine: 12 Day Officers (建除十二神) — officer = day branch measured from month branch, keyed at local noon; jié-boundary repeat asserted as a property; golden anchor 2026-06-21 = 丙寅/甲午 = 成 from the product owner's Sinarmas 2026 almanac reference (favors match the printed page). 27 new tests, 155 engine tests green
- [x] Engine: per-activity dayQuality (officer 宜/忌 ±2, favourable-element day +1, personal breaker/combine on the home palace, career/roots clashes — interpretive one-school, hour pillar never consulted, unknown-time safe); horizonFacts (流年/流月, new "monthly" transit palace); findDates (≤366-day range, 1–2 charts, combined = min, ties → earlier date)
- [x] Content: VOICE.md rule 12 (layered guidance: Favors/Watch chips + weather prose + soft directives, postponement never prohibition); OFFICER_GLOSSES (12) + ACTIVITY_LABELS (10, modern label glossed by the classical category); dayGuidance (chips capped 3/side, |score| order, every friction chip explained in prose), horizonReading (流年/流月), dateVerdictLine — seeded-deterministic, fact-tagged, voice-swept; 93 content tests green; orchestrator spot-checked rendered lines on Fixture A (成 day, breaker day, worst-day verdict)
- [x] Web: Today Favors/Watch chips + cited prose + 7-day tone strip (ink/paper markers, tap-to-jump, no traffic lights); Cycles This year/This month outlook; /dates/ finder (activity → range ≤366d clamped to table years → optional saved person) with per-chart leaning swatches — favours = wood green, friction = fire orange #D0662A (cinnabar stays seal-only per 2026-07-07 decision), verdict line under the top pick; quiet entry links on Today and Compare
- [x] E2E: 5 new specs (chips + cited line, week-strip jump, Cycles 丙午, finder ranking + officer, two swatches with saved person); 20/20 total; full pnpm verify green (155 engine + 93 content tests); /dates/ in SW precache (67 files)

## M14 — Glossary, read-more deep dives & Co-Star Today arrangement (2026-07-13/14)
- [x] Glossary layer: GLOSSARY keyed by ReadingLine.topic (every line builder sets one), assembled from the canonical glosses; FactTag caption component links every fact tag to a GlossarySheet bottom-sheet explainer; entries voice- and strip-checked (glossary.test.ts)
- [x] Read-more deep dives: READ_MORE (read-more.ts, same topic keying) — essays on the reading as lived, closing with a rule-12 "Working with it" section; never re-teach the category (tested); all five interaction types covered (read-more.test.ts); GlossarySheet renders both shapes
- [x] Voice refinement (three owner rounds): ten-god lines open on the classical name framed as a name, then translate in full; "the old calendars/old books" as source words; branch runs ride as branchToken so Han strip keeps sentences whole; TEN_GOD_PERIOD_THEMES richer per-god horizon themes
- [x] Co-Star Today arrangement (owner-picked from three mockups): headline hook in display type (banks/headlines.ts — transit-first keying, no Han, cites nothing); Favors/Watch merged into one two-column DayBoard (chips + fact-cited suggestions over explaining prose); "Your day, by area" AreaGauges rows for all 10 activities (score-magnitude meters in finder tints, tap unfolds activityAreaLine reusing chip frames; NEUTRAL_AREA_FRAMES for even rows); week strip demoted below areas; agency line still last
- [x] Chrome: favicon.ico generated into the icon pipeline; headline/glossary/read-more E2E (glossary.spec.ts); full verify green, 25/25 E2E

## M15 — Design refinement pass (2026-07-16; owner: unhappy with design/UI/layout — calm minimal direction)
- [x] DESIGN.md revised: four-register type ladder (display / body 15px-ink / kicker with 24×2 ink column-rule / caption), captions-are-metadata-not-links (chevron affordance, underlines nowhere), ruled-prose vs boxes surface system, forms spec (48px fields, segmented controls, selected states, unpressable-not-broken disabled), dark fill-not-border elevation rule, layout section brought up to the real app (5-tab nav, board, gauges, finder)
- [x] Primitives: .kicker/.caption/.field-input in globals.css; ReadingCard → ruled prose block (parents stack with divide-y); FactTag chevron caption; Button disabled restyle; SegmentedControl (sex, appearance — role=radio preserved for E2E)
- [x] Today: 56px day-pillar hero with element-dot caption; reading as ruled prose; guidance prose grouped by fact tag (kills the triple "成 Success day" card stack); neutral gauge rows behind "Show all N areas"; week-strip kicker; quiet arrow links
- [x] Forms: onboarding primary action in flow (dead void gone), bigger progress dots, all inputs on .field-input, Compare/sex segmented, finder activity cards with ring selected state and nowrap Han glosses
- [x] Chart/Cycles/Compare/Dates: all section headers on the kicker system; Cycles horizons de-boxed to ruled prose; collapsible chart sections ruled; dark theme boxes borderless (fill elevation), hairlines dividers-only
- [x] Verify green; 25/25 E2E (dates-guidance updated for the gauge disclosure); every screen re-screenshotted light+dark at 390px and reviewed against the revised DESIGN.md
- [x] Reviewer verdict folded in (REVISE → all 13 issues): DESIGN.md contradictions fixed (interactive controls carve-out from dark elevation + Boxes list, underline rule reworded around the day-column marker, board = two kicker sections, Han/16px-input named exceptions, agency top-border named sole decorated box, Compare + /dates/ layout lines, annual-row inner-scroll wording); finder picker rebuilt as a raised divided list (hairline = dividers only, selected = 2px inset ink ring); ≥44px tap targets + pressed state on caption rows and arrow links (.tap-target; real padding where neighbors are interactive); gauge-disclosure supersession logged in CLAUDE.md. Contrast math verified correct by reviewer, no token changes.

## M16 — Material look-and-feel (2026-07-16; owner: Google-like UI, "full Material feel", everything in tonal cards)
- [x] DESIGN.md v3 (supersedes M15 surface language, keeps its a11y wins): --surface tonal containers borderless in BOTH themes (light #EAEDE9 — ink-soft 5.63:1, element hues ≥3:1; dark #20252F — ink-soft 6.73:1), separation = fill + gap (hairline demoted to rare in-container dividers), radius scale 24/28/16/pill, segment-stack list idiom, chips = 10% ink tint (color-mix — tokens are raw hex, no alpha plumbing), Figtree replaces Fraunces+Inter (display = weight not a second face; 16px inputs stay the iOS-zoom exception), sentence-case section headers (ink column-rule retired), springy selection/sheet motion (240ms overshoot curve, reduced-motion gated)
- [x] Implementation: .card/.stack/.chip/.bg-ink-tint primitives; reading lines + guidance groups + horizons + results as tonal cards with 8px gaps; gauges/picker/settings/saved-people as segment stacks; pill buttons, pill segmented controls, tint person-chips, pill week cells (spring-in select); sheets slide up at 28px radius; bottom nav + SW toast on surface fill; agency card keeps the ink top-border exception; meter empty cells ink-tint
- [x] Kept: ink/cinnabar palette, seal, element hues, voice, layout order, ink-soft control borders both themes, ≥44px tap targets + pressed states, guidance grouping, gauge disclosure

## M17 — Co-Star content restructure (2026-07-16; owner: "rearrange the layouting… original inspiration was Co Star"; loop = research → execution → testing)
- [x] Research: live-web Co-Star teardown synthesized with first-hand knowledge into docs/research-2026-07-16-costar-layout.md — hero statement, at-a-glance chart before prose, per-area sections with the citation UNDER the prose, stark do/don't lists; kept out: Do/Don't naming (rule 12 holds), brutal tone, black-and-white severity (M16 skin stays)
- [x] Content: ReadingLine.area (Palace | "overall") set by the daily assembler — transit lines file under their first natal palace, day-level lines under "overall"; area.test.ts; 124 content tests green
- [x] Web: Today reordered — date strip → headline+pillar hero → "At a glance" (axis-dot rows: dot offset from center tick = leaning strength, wood/fire tints, neutral hollow) → reading as life-area section cards (Roots/Career/Home/Horizon/The day itself, citations below prose, ReadingCard citation="below") → Favors/Watch as stark word lists (pill chips retired, hairline divider before suggestions) → week strip → agency; palace-touch sentence retired (headers carry it)
- [x] Testing loops: loop 1 verify + 25/25 E2E + both-theme screenshot critique (found the 立券交易 mid-token wrap + read-more/arrow-rule tension); loop 2 fixed both, re-shot, re-gated green

## M18 — Design reset (v2 arc stage 1; approved 2026-07-29 — see docs/discussion-2026-07-23-rewrite-and-roadmap.md)
- [x] Owner inputs collected — references: Co-Star (whitespace), Fitbit/Google Health (soft info presentation, screenshot supplied), How We Feel (diagrams); open to an entirely new palette; mockups together before commissioning a designer. Reference research in docs/research-2026-07-29-design-references.md (4-agent sweep)
- [x] Three throwaway HTML mockups of the same Today screen, real Fixture A data (2026-07-29, Wood-dragon Receive day, real engine+content output), both themes, mobile viewport — docs/mockups-m18/today-three-treatments.html (directions re-aimed at the owner's "calm data" synthesis: 1 Daybreak editorial-air, 2 Sky soft-dashboard, 3 Compass diagram-led; all element-color-derived)
- [x] Mockup rounds 2–5 in the same file (owner feedback loop; Dribbble picks distilled in docs/mockups-m18/dribbble-reference-analysis.md): round 2 restructure (animal week strip, merged day's-grain block), round 3 Compass II (warm cream, petal-flower hero, black anchors), round 4 Forecast (the day as a weather report), round 5 Trail (freestyle new concept: the day as a route on a topographic map — olive-paper/pine-ink palette, Bricolage Grotesque + Space Mono over Figtree body, contour-map hero with the clash as a marked crossing, 7-day elevation profile, agency line as a directional trail signpost; keeps the Dribbble DNA of one black anchor + mixed-style headline + dashed accents). Round 5b (owner likes Trail, wants a per-day palette): terrain palette keyed to the day pillar's element — 5 terrains (Wood forest / Fire canyon / Earth dune / Metal granite / Water nautical), light+dark each, deterministic per date; data hues and signage colors stay fixed so color never changes meaning; zodiac-keyed (12) considered and set aside — grounds blur and the animal already carries the day's identity; mockup has a preview capsule to flip palettes since the fixture day is locked to Wood
- [x] Round 5c both-theme pass (owner reminder: "we have a light and dark mode"): Appearance control (Light / Dark / System, persisted) added to the mockup chrome so both themes are reviewable on the phone regardless of the OS setting — forcing a theme verified to resolve identically to the matching OS scheme on all 10 screens (computed-style diff of every element, scripted). Audit found all 34 text-contrast failures were in LIGHT mode (muted caption tone + element hues at 8.5–13px; worst 3.07:1 = Daybreak "Watch" heading) — light hues deepened 5–15% at the same hue, all six treatments now clear AA (4.5 body / 3.0 large) in both themes, Trail across all five terrains. Dark-mode design fix: the Dribbble "one black anchor per screen" device (Trail/Forecast/Compass II agency line, today pill, nav) sank into the dark ground, so the anchor pair inverts in dark — paper mass, ink text — keeping it the boldest object rather than the darkest; anchors tokenized (--blk/--pale) instead of hardcoded pale hexes. Two rules were themed only via the OS media query (Compass's worth-doing/postponing columns, Sky's nav shadow) and one light twin was missing --pale — the toggle now reaches everything
- [x] Effects exploration (glassmorphism/liquid glass, neumorphism, gradients — docs/research-2026-07-30-effects-glass-neumorphism.md + docs/research-2026-07-30-effects-gradient-other.md): a fourth mockup, docs/mockups-m18/effects-exploration.html, isolates surface/material effects as a single variable against ONE shared layout (Daybreak's) so the owner judges effects, not layout, across 5 tabs — Baseline (control), Glass accents (backdrop-filter restricted to bottom nav + week-strip icon chips only, per the research verdict that glass never touches the hero or reading prose), Gradient extended (the hero's data-driven element gradient propagated to week-strip cells, keyed to each day's branch element; orbit badge gets a one-time settle-in morph on tab-show), Neumorphic accent (soft-shadow-plus-real-border hybrid on the orbit badge only), and Overdone (why not) — a deliberate, in-UI-flagged counter-example (full-screen frosted glass, unbordered neumorphic favors/watch board, contrast left uncorrected on purpose). All non-Overdone tabs computed-contrast-checked in both themes (nav/week-cell glass pairings verified 7.4–9.4:1 against the app's own background range). Recommendation: carry Gradient extended into a real mockup round; Neumorphic accent as a minor detail pass; Glass accents is defensible but not a strong default.
- [x] On-device review; owner picked Trail (2026-08-05); rejected directions + reasoning logged in CLAUDE.md
- [x] DESIGN.md v4 rewritten from the Trail system (tokens both themes × 5 terrains with computed WCAG ratios via docs/design-system/check.mjs, type ladder, shape scale, motion, component inventory, map-hero/elevation-profile determinism spec) — reviewer critique: REQUEST_CHANGES → fixed (1 major, 4 minor). Major: §Concept's "one black anchor per screen" contradicted the always-present anchor-filled bottom nav; resolved by splitting the anchor pair into a fixed chrome anchor (the nav, outside the per-screen budget) and a per-screen content anchor (signpost/primary button) — §Concept and §Surfaces reworded, no visual change. Minors: map-hero waypoint 0/1-citation layouts now spelled out; disabled-button and segmented-control track wording corrected to match the validated docs/design-system recipe (card/paper-mix + line border, not ink-tint; ink-soft/mut track tint, not ink) — this also caught a real near-miss contrast bug in the shipped components (ink-tint segmented track measured ~4.25:1 against ink-soft labels in wood/light, under the 4.5 floor), fixed in globals.css (.bg-mut-tint) and segmented-control.tsx; 360px floor note clarified as NOT covered by check.mjs's fixed 430px viewport (manual/Wave-4 item)
- [x] Scope decision (owner, 2026-08-05): reskin apps/web in place rather than fold in the M19 structural rebuild — "zero app code" superseded, see CLAUDE.md

## M18.5 — Trail rollout (reskin apps/web in place; 2026-08-05—)
- [x] Wave 1 — Foundations: Bricolage Grotesque + Space Mono added alongside Figtree (`next/font/google`); globals.css tokens replaced with terrain-ground × theme + data+signage hues + shape/shadow scale (`apps/web/scripts/generate-tokens.mjs` → `tokens.generated.css`, transcribed from docs/design-system/src/tokens.mjs); `data-terrain` computed client-side from the day pillar's element in ProfileGate (no pre-paint script needed — terrain has no meaning before onboarding); shared primitives restyled (button incl. new quiet/ghost variants, field-input, segmented-control, card/stack, bottom sheet); found + fixed a real bug in the token generator (the wood/light fallback never responded to dark mode before a terrain was stamped — fixed by giving the pre-terrain fallback its own theme-keyed ground); full `pnpm verify` green, dev server sanity-checked on-device via Playwright screenshots (light/dark, disabled/enabled button, segmented-control selection)
- [x] Wave 2 — Today rebuilt: new datebar.tsx (mono date + compass mark, keeps all prev/next/jump interactivity), elevation-profile.tsx (replaces week-strip.tsx), legend-tags.tsx (element·polarity + zodiac·day-type; third "officer" notice tag dropped — OFFICER_GLOSSES only has full sentences, not chip-length copy, and inventing one was out of scope), map-hero.tsx + lib/route-waypoints.ts (replaces day-orbit.tsx — real per-day waypoints matched back to their ReadingFact for the true transit branch, crossing-vs-plain from InteractionType, dayTone-colored highlight segment; route geometry is a faithful-but-simplified port of the mockup, not pixel-identical), waypoint-rail.tsx (replaces reading-area-sections.tsx, same M17 area-grouping + prose-then-citation rule), trail-signs.tsx (replaces day-board.tsx; "Clear trail"/"Take it slow"), signpost + streak inlined in today-view.tsx (replaces the agency card), bottom-nav.tsx restyled as the anchor-fill pill. area-gauges.tsx deleted (DESIGN.md explicitly retires the old "At a glance" gauges). `tsc`/`lint` clean.
- [x] Wave 3 — Chart, Cycles, Compare, Dates, Settings, Onboarding: audit-and-patch pass (most screens already inherited Trail for free via the Wave-1 primitives). Real gap found: no shared class expressed DESIGN.md's "Space Mono uppercase label above" for form fields — added `.field-label` to globals.css, applied to compare-form.tsx (5 labels) and onboarding date-step/time-step. One judgment call resolved: city-search.tsx's results dropdown (16px, no clean token match) set to `rounded-field` — it extends a field-input visually, so it reads as the field's own radius rather than a card/tile. `tsc`/`lint` clean.
- [x] Wave 4 — Verify: `pnpm verify` green; E2E updated for the new UI (3 of 24 specs — Favors/Watch→Clear trail/Take it slow copy, and date assertions moved from visible text to the datebar's aria-label since the visible format changed to a short mono date), 24/24 passing. Rendered-contrast check (check.mjs's methodology, adapted to drive the real built app via seeded profile + pinned clock across all 5 terrains × both themes, not just the design-system prototype) found and fixed 3 real AA failures the prototype hadn't caught: elevation-profile's day labels were fading below AA along with their distance-fade icon (fixed — only the icon fades now, the label stays full-strength); segmented-control's unselected label was ink-soft on its own ink-soft-tinted track, ~4.0:1 (fixed — unselected label is full ink now); legend-tags reused the shared `.chip` class whose ink-tint background was too dark for ink-soft text at 9.5px (fixed — legend tags use a plain `bg-surface` pill instead, matching the mockup's own untinted chip). Re-verified 0 failures across /today/, /chart/, /cycles/, /compare/, /dates/, /settings/ × 5 terrains × 2 themes after the fixes. A second code-review pass (correctness/a11y/dead-code/token-fidelity, not just the design doc) returned APPROVE with 2 minor cleanup items, both fixed: retired M17 `.hero-card` ink-wash CSS (dead since MapHero took over the Today hero) deleted from globals.css; `layout.tsx`'s PWA `themeColor` still hardcoded the old M16 paper/ink pair, updated to Trail's wood-terrain ground. Final `pnpm verify` + full E2E re-run green after both fixes.

## M19 — apps/web rebuild (from-scratch, owner-confirmed over an in-place alternative; started 2026-08-05 — see CLAUDE.md decisions A-F, full phase plan in /Users/Tipitaka/.claude/plans/woolly-hugging-taco.md)
- [x] Phase 0 — Decisions A-F locked and logged in CLAUDE.md; screen-parity checklist confirmed against the current route list (/, /chart, /compare, /cycles, /dates, /onboarding, /settings, /settings/edit, /today; 5-tab nav)
- [x] Phase 1 — packages/presentation scaffolded (mirrors packages/content exactly); 18 pure files extracted from apps/web/src/lib, 67 tests, 99.51% line coverage. date-finder.ts moved whole (no content dependency, not just its maps); share-card.ts left entirely in apps/web (canvas geometry entangled with measureText() — not a clean split). Found + pinned (not fixed, out of scope) a pre-existing date-finder.ts bug: MIN_DATE/MAX_DATE literals don't match the engine's actual solar-term table bounds, so range searches clamped to either literal throw instead of trimming as documented. compare.ts's seed-key dependency temporarily duplicated (packages/presentation/src/seed-key.ts) with a note for Phase 5 to fold reading.ts over to it. Root `pnpm verify` green.
- [x] Phase 2 — ContentRun/TokenLine type (packages/content/src/tokens.ts) proven on vocab.ts's new branchTokenRuns/interactionTagRuns and banks/transit-days.ts's TEN_GOD_LINES (10 entries authored as real structured data, VOICE.md rule-11 framing intact). ReadingLine.runs/factTagRuns additive; dailyReading() gives every line a mechanical text-run fallback until its bank is migrated. coverage.test.ts's old strip-then-substring assertion rewritten as a structural runs-shape assertion; new assertGlossed() structural helper. strip-han.ts/test untouched (old app still needs them). Verified: content 121/121, old app's full E2E 24/24 green — additive strategy holds.
- [x] Phase 3 — apps/web-next scaffolded (config mirrors apps/web's next.config.mjs webpack shims verbatim). Single-store data layer: daymaster.store.v2, migration on first loadStore() ingesting compare.v1→people.v1/people-active.v1→profile.v1→theme.v1 in order, idempotent, drops stray fields (e.g. legacy han) automatically, leaves streak.v1 alone; backup v2 = same shape; deleteAllData() clears store.v2+streak.v1+the two sessionStorage keys. 7-test migration suite (hand-rolled FakeStorage, no jsdom dependency added). Pre-paint theme script reads store.v2.theme with a fallback to the legacy key (for pre-migration devices) — the one deliberate deviation from "copy verbatim". Placeholder routes for all 9 screens + bare bottom-nav. One post-landing fix: store-types.ts's Sex/StoredCity/StoredBirth/StoredConfig/StoredProfile/StoredPerson were duplicated from packages/presentation rather than imported (flagged by the agent as a TODO since presentation hadn't landed yet when it started) — deduplicated once Phase 1 confirmed those types were exported; app-local ThemePreference + validators/sanitizers stay in apps/web-next. Root `pnpm verify` green; confirmed zero edits under apps/web/ (old app untouched, git status shows only pre-existing M18.5 changes).
- [x] Phase 4 — Remaining content bank conversions to runs, went further than the explicit list once the gap was visible: real runs on banks/stars.ts, banks/stages.ts, banks/transit-interactions.ts, compare-reading.ts, vocab.ts's new STRENGTH_CHECK_GLOSS_RUNS (classical code as the term, e.g. `{term:"得令", han:"得令", gloss:"in season"}`, since there's no separate English name), glossary.ts's ten-gods entries, PLUS day-guidance.ts (officerLine/evenDayLine/explainLine/activityAreaLine/dateVerdictLine) and horizon-reading.ts (themeLine/elementLine/transitLine) so every ReadingLine anywhere is runs-complete, not just the originally-named banks. New shared `fillRuns()` template-splicer in tokens.ts (the runs equivalent of the existing string `fill()`). All 4 target test files converted to structural runs-shape assertions. Verified independently: content 121/121, root `pnpm verify` green, old app's full E2E 24/24 unchanged.
- [x] Phase 5 — Content-dependent presentation view-models: `todayScreenModel(profile, dateISO, todayISO)` collapses today-view.tsx's 5 useMemo calls + inline derivation into one typed return (chart/pillars/dayPillar/stem/branch/reading/guidance/tone/waypoints/headline/grainLine/branchByArea/dateRange); elevation.ts, chart-preview.ts (kills the reveal-step/edit-birth-flow duplication), pillar-stars.ts, map-hero.ts, guidance-board.ts, renderRun (ContentRun+stable key, pass-through since daily-reading.ts already guarantees every line's `.runs` is populated). natalSeedKey dedup resolved (presentation's reading/guidance/date-finder all import the one copy from seed-key.ts; apps/web's own copy stays, retired wholesale at cutover not folded in). route-waypoints.ts wart fixed for real, not just flagged: `interactionTag` was already in vocab.ts internally, just not re-exported — added a one-line additive export to content/src/index.ts, presentation now imports it instead of maintaining a private duplicate word map. share-card.ts confirmed still not cleanly separable from Canvas/DOM calls (same call as Phase 1), left in apps/web. 125 tests, 99.68% line / 92.1% branch coverage. Verified independently: presentation 125/125, root `pnpm verify` green.
- [x] Phase 6 — UI Wave 1: primitives ported verbatim from apps/web's already-verified Trail implementation (button, segmented-control, globals.css components layer + motion, glossary-sheet chrome, glyph-icon now reading path data from presentation, bottom-nav restyled to the anchor pill). Onboarding fully ported and rewired: reveal-step now uses presentation's `chartPreviewFor` (kills the old duplicated try/catch pattern), added `saveProfile()` to store.ts as the real completion write path, restore-from-backup placed on the first onboarding step (apps/web never had an alternative placement — settings-content.tsx only exports). Verified end-to-end with a scripted Playwright click-through against the live dev server: full 5-step flow → save → redirect to /today/ → read back a well-formed daymaster.store.v2 with the correct profile, zero console errors. Deliberate gap: share-link stashing not wired (nothing to consume it until Compare exists in Phase 8; SHARE_INCOMING_KEY reserved in store.ts). apps/web confirmed untouched (git status diff count unchanged). Root `pnpm verify` green.
- [x] Phase 7 — UI Wave 2: Today screen, full port of the old app's verified Trail implementation into apps/web-next, data sourced from presentation's todayScreenModel/elevationWeek/elevationPath/mapHeroSummary/guidanceBoardFor instead of ad-hoc useMemos; new TokenText component + content-runs.ts helpers are the one place a TokenLine becomes rendered text (term run → gloss only, never han, per decision F). Real content-package gap found and worked around locally: daily-reading.ts's suggestion-candidate dos/donts factTags are built from raw Han fact data and never get factTagRuns authored, so their mechanical fallback wraps unstripped Han — TokenText/plainText defensively strip text-run strings (never term-run glosses) as a safety net; confirmed via a live check that 0 CJK characters render across 5 terrains × both themes. Real fix (author factTagRuns in suggestionCandidates) deferred to Phase 11 cleanup, when the fallback string path retires entirely. app-shell.tsx/profile-gate.tsx built fresh this phase (Today is the first gated main screen). Reviewer pass: **APPROVE**, zero issues — confirmed data wiring is real (not stale/hardcoded), the term-run-never-renders-han contract holds everywhere including reading-card.tsx/fact-tag.tsx/glossary-sheet.tsx, a11y correct (map-hero's synthesized aria-label genuinely present), zero hardcoded hex/pixel values, zero dead code/TODOs. One inert note: today-view.tsx's headline renders via headlineRuns() directly (bypasses TokenText) — identical to the old app, and packages/content's headlines bank has zero Han, so harmless; worth remembering for Phase 11's audit. Root `pnpm verify` green, apps/web confirmed untouched (reviewer independently re-ran the build).
- [x] Phase 8 — UI Wave 3: Chart, Cycles (+horizon-outlook/luck-timeline/annual-row), Compare (+share-incoming wiring), Dates finder, Settings (+/settings/edit), share card/link all ported into apps/web-next, data sourced from presentation (chartFor/natalReadingFor/horizonBundleFor/compareBundleFor/findDatesFor/chartPreviewFor) and prose rendered through TokenText, matching Phase 7's established pattern. store.ts gained people/theme read-write helpers (loadPeople/addPerson/removePerson/setActivePersonId, load/saveThemePreference) plus `applyThemePreference` (stamps `data-theme` immediately on an Appearance change — the store-only version silently didn't touch the DOM, since Phase 3's pre-paint script only reads on boot). share-card.ts/share-link.ts ported with the font custom properties corrected to Trail's actual `--font-bricolage`/`--font-figtree` pair (the old app's copy still pointed at M16's retired `--font-fraunces`/`--font-inter`, silently falling back to generic serif/sans-serif). Real cross-app bug found and fixed here: `--cinnabar` was referenced by tailwind.config.ts/seal.tsx/glossary-sheet.tsx but never actually defined by the Trail token generator or globals.css in EITHER app, so the seal — DESIGN.md's one explicitly fixed identity element — silently rendered plain black instead of cinnabar red; fixed in apps/web-next's globals.css (`--cinnabar: #bf3a2b`, matching the 2026-07-07 decision log value), confirmed only seal.tsx references it (seal-only rule holds); apps/web carried the same gap — since it's the still-*live* app (not just an M19 in-progress build), fixed there too immediately after this phase reported it (same one-line addition to apps/web's globals.css `:root`), confirmed via the running dev server that `--cinnabar` now resolves to `#bf3a2b`, then a full clean rebuild (killed the stale `next dev` holding `.next`, cleared it, `pnpm verify` green including apps/web's own build, full 24/24 E2E re-run green, dev server restarted on 3001). Verified: `tsc`/`lint`/`build` clean for apps/web-next (all 9 routes static-export); root `pnpm verify` green for every package including apps/web (its earlier build failure was confirmed pre-existing/unrelated environmental contention from the stale dev server, not a code defect — resolved by the same cleanup). Rendered-contrast pass (check.mjs's methodology, adapted to drive the live static-served app via a seeded FIXTURE_A store + pinned clock): 2550 text runs measured across the 6 newly-built routes × 2 themes × 5 terrains, 0 AA failures. A Playwright smoke pass across all 7 gated routes × both themes found 0 console/page errors with real seeded data (profile, a saved person, an activity search). A second, independently-dispatched agent instance overlapped on this same task (a task-board naming/ownership quirk, not intentional double-dispatch); it verified the first pass's work before extending it rather than redoing it, and its own broader contrast sweep (5,730 runs across 10 screen-states incl. dates-results and settings/edit, vs. the first pass's 2,550) caught one more real bug the first pass's narrower state coverage had missed: date-results.tsx's per-chart leaning label (`text-ink-soft` at 10px on a tinted leaning background) measured 3.14–3.65:1, fixed to full `text-ink`. Also added `packages/presentation/src/horizons.ts` (horizonBundleFor/HorizonBundle, additive, Cycles needed it and no prior phase had extracted it) and confirmed Settings genuinely has no restore-from-backup entry point in the old app either (onboarding-first-step-only is the real precedent, not a gap). Re-verified after both passes: root `pnpm verify` green, apps/web still untouched, PWA precache (Phase 9) intact at 67 files.
- [x] Phase 9 — PWA re-establishment: generate-sw.mjs/generate-icons.mjs/generate-manifest-screenshots.mjs ported (the manifest-screenshot generator's localStorage fixture updated for the new store.v2 envelope shape), sw.js template + service-worker.tsx (install-and-wait, the `accepted` ref guard against the first-install controllerchange firing an unprompted reload — verified present and correct) byte-identical to the old app, real manifest.webmanifest with icon variants/shortcuts/screenshots. theme_color/background_color (#F0EEE2) verified against tokens.generated.css's actual wood/light default, matches layout.tsx's viewport.themeColor (already correct in web-next, unlike the M18.5 bug in the old app). Verified independently: clean build produces a real 67-file precache list (grepped and read directly, not assumed), offline navigation to an unvisited route confirmed working via a scripted Playwright check, root `pnpm verify` green, apps/web untouched (35-file diff count unchanged, matching every prior check this session).
- [x] Phase 10 — E2E suite rebuilt against apps/web-next's DOM on port 3211 (old app keeps 3210): 10 old specs ported (seedProfile/seedCompanion rewritten as read-modify-write against the single store.v2 key so call order composes correctly, unlike the old app's six independent keys; the datebar aria-label disambiguation trick carried over unchanged since Datebar is a faithful port; compare.spec.ts's legacy-migration test now exercises store-migration.ts's real path since there's no standalone people.ts anymore) + 4 new store-migration.spec.ts tests (full six-key migration, stray retired `han` field dropped without crashing, compare.v1-only → "Them" + active, compare.v1 appending after existing people.v1 and outranking the legacy active pointer). 28/28 green. Verified independently: both suites re-run in the same session (old app 24/24 on 3210, new suite 28/28 on 3211) with no port collision; apps/web untouched.
- [x] Phase 11 — Reviewer pass: REQUEST_CHANGES → fixed → sign-off CONFIRMED (see CLAUDE.md decisions log for the full finding: a missing en-dash separator between concatenated branch term runs was fusing adjacent glosses into one word under TokenText's gloss-only render mode; fixed with a shared `joinBranchRuns()` helper + a new permanent regression test, plus two minor cleanups). Separately closed the one deferred content gap Phase 7 flagged: `daily-reading.ts`'s `suggestionCandidates()` now authors real `factTagRuns` for interaction/star-based dos/donts instead of relying on the mechanical text-wrap fallback (which didn't strip Han) — new regression test in coverage.test.ts confirms every dos/donts fact tag renders gloss-only with zero raw CJK. Full `pnpm verify` (run step-by-step after two `pnpm verify` timeouts traced to environmental noise — a stray dev server holding `.next`, then vitest's own ~80s shutdown quirk in apps/web-next, neither a code defect) green across all 6 packages; both E2E suites re-confirmed (new app 28/28; old app 24/24 after one isolated flaky re-run of a date-jump interaction test, confirmed non-reproducing). **Content cleanup (closed out)**: `ReadingLine.text`/`.factTag` string fields, `strip-han.ts`/`strip-han.test.ts`, and the string-returning `branchToken`/`interactionTag` deleted for real — `runs`/`factTagRuns` are now the only path package-wide, via a `DraftLine`/`finalizeLine()` boundary (every bank/assembler builds an internal draft shape, `finalizeLine()` is the one place it becomes the public `ReadingLine`). Two more real Han-leak bugs surfaced only once the defensive `stripHanCharacters` safety net came off: `compare-reading.ts`'s `dayMasterLines()` had two fact tags built from raw, un-glossed Han with zero `runs` authored at all (fixed with a new `stemTokenRuns()` helper + proper `TEN_GOD_CHINESE`-sourced term runs); `natal-interactions.ts`'s body text never authored `runs` for its `{branches}` placeholder either, filling it with raw `input.branches.join("")` (fixed via `fillRuns()` + `joinBranchRuns()`). A third bug found by the test-migration pass itself: `horizon-reading.ts`'s `themeLine()` collapses `"{tgEn} ({tgCn})"` into one term run assuming every `THEME_FRAMES` template keeps that pair contiguous, but `THEME_FRAMES[1]` had `" note "` splitting them (`"...the {tgEn} note ({tgCn}) of..."`) — the collapse silently failed to match and `{tgEn}`/`{tgCn}` leaked as literal unrendered braces into any annual/monthly reading that picked this template (verified live with english "Rob Wealth"/seed "seed"); fixed by reordering the template's wording (`"...the {tgEn} ({tgCn}) note of..."`) to restore the invariant the code already assumed. All ~90 resulting test-file compile errors across 10 `packages/content` and 5 `packages/presentation` test files fixed mechanically (new `lineText()`/`lineFactTag()` test helpers in `packages/content/test/token-utils.ts`, inline `plainGloss()` calls in presentation's tests) with zero assertion-intent changes — some needed structural `runs`-shape rewrites instead of substring matches, since gloss-only rendering changed what's literally in the string (raw Han → English glosses, e.g. compare's day-master tag `"戊 × 甲 · day-masters"` → `"yang-earth × yang-wood · day-masters"`). Final state fully reverified end-to-end: `apps/web-next`+`packages/{bazi-engine,content,presentation}` all green (typecheck/lint/401 unit tests/build/28 E2E); `apps/web` now fails typecheck exactly as expected (27 errors, all `.text`/`.factTag`/`stripHanCharacters` — proof the old string API is genuinely gone, not silently tolerated), which is correct since it's deleted next in Phase 12, not fixed.
- [x] Phase 12 — Cutover: old apps/web deleted, apps/web-next moved into its place (see CLAUDE.md for the directory-nesting fixup this needed), package identity + ports + doc comments updated for the new location, `pnpm-lock.yaml` refreshed. Fully reverified at apps/web: `pnpm -r typecheck/lint/test/build` green, 28/28 E2E on port 3210 (no longer needs 3211's coexistence reservation), live `next dev` sanity check across all 7 routes (200s, real content, not error pages). Deployed to production (`pnpm --filter @daymaster/web build` → `vercel link`/`vercel deploy --prod` from `apps/web/out`, per the CLI static-deploy setup — no Vercel dashboard change needed, see CLAUDE.md's correction to decision D): live at https://daymaster-nu.vercel.app, verified via curl (all 7 routes 200, real `<title>Daymaster</title>` markup, sw.js precache hash matches the fresh build). **M19 complete** — apps/web is now the from-scratch rebuild (packages/presentation, structured content tokens, single versioned store, Trail visual identity) end to end in production.

## Flags / unverifiable values
- Day-officer 宜/忌 activity table (data/day-officer-tables.ts) is INTERPRETIVE: the officer sequence and month/day-branch rule are standard (協紀辨方書 lineage; cross-checked against wonyanconsult.com and fourpillars.pro, 2026-07-08), but per-officer activity lists vary by almanac publisher. Ours is a conservative common core; one printed-almanac golden anchor (Sinarmas 2026, 2026-06-21 成 day) is asserted in tests. Refine if more printed pages become available.
- Equation of time uses the sun's geometric mean longitude from the standard Meeus polynomial (280.46646 + 36000.76983·T + 0.0003032·T², Astronomical Algorithms ch. 25) because astronomy-engine exposes only apparent RA. ACCEPTED: standard published constants, source-commented in src/true-solar-time.ts, validated against known EoT extremes (±20 min bound, Nov ≈ +16.5 min).
- 神煞 學堂, 詞館, 血刃 are OMITTED: the reference app (master-reading screenshot, 2026-07-08) places 學堂@卯, 詞館@申, 血刃@戌 for Fixture A, and none of the classical rule variants I could verify (三命通會 day-stem or nayin-命 keyings) reproduce those placements. Rather than guess a school, they're left out; add once the app's rule can be confirmed (its 神煞 help screen, or more example charts).
- 命宮/身宮 (life/body palace) OMITTED: the formula is school-variant (month/hour indexing differs by lineage) and the reference screenshot doesn't show the 命身胎息 tab, so no golden value exists to pick a school against. 胎元 (single unambiguous rule) IS implemented.
- Luck-start day precision matches the reference app exactly (9y5m25d) when the birth wall-clock is UTC+8; at Asia/Jakarta (UTC+7) the same rule yields 9y5m20d. Both asserted in tests. The pillars, stages, and stars are timezone-insensitive for this chart; only the 起運 day count shifts.
